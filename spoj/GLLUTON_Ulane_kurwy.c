#include <stdio.h>
#include <stdint.h>

define H_24 = 86400;
int CookiesTotalValue(int n,int tab[n])
{
    

}

int main() 
{
    int t;
    scanf("%d", &t);

    for (int i = 0; i < t; i++) 
    {
        int N,M;
        scanf("%d %d", N, M);
        int tab[N];
            for(int j = 0; j < N; j++)
            {
                scanf("%d", tab[j++]);
            }
    }

    return 0;
}
